node_modules/
dist/
*.log
.DS_Store
.env

# Generated runtime artifacts (regenerate with `npm run simulate`).
# A committed sample run lives under outputs/ so the repo shows results;
# local re-runs overwrite these and are ignored below if you prefer clean diffs.
outputs/travel_os.db
# Large regenerable artifacts at full-coverage scale (2,348 nodes).
outputs/edges.json
outputs/risk_heatmap.json
